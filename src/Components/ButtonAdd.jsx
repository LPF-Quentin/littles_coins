import React from 'react';

const ButtonAdd = () => {
    return (
        <div>
            <input type="text" placeholder='quantite'/>
            <button target="_blank">add</button>
        </div>
    );
};

export default ButtonAdd;